/*
 * pet.cpp - Pet class
 */

class CPet {
  CPet(char[] nom, int yrs) {
    if (strlen(nom) > strlen(name)
        fprintf(stderr, "Warning: Length of pet name is too long\n");
    strncpy(name, nom, strlen(name));
    age = yrs;



  }

  int set_age(int yrs) {
    age = n;
    return(age);
  }




};

